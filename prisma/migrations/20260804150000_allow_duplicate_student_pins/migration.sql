-- Ученик сначала выбирает конкретный профиль, поэтому PIN проверяется только
-- для выбранного userId и не обязан быть уникальным среди остальных учеников.
-- Открытые PIN по-прежнему не хранятся: для входа остаётся bcrypt-хеш.
DROP INDEX IF EXISTS "User_pinFingerprint_key";
