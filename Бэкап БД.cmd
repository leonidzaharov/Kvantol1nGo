@echo off
chcp 65001 >nul
rem ============================================================
rem Ручной бэкап базы данных — двойной клик и готово.
rem Снимок всех таблиц падает в папку backups\ (JSON).
rem Восстановление: npm run restore (сначала сухой прогон).
rem ============================================================
cd /d "%~dp0"
echo Снимаю бэкап базы...
echo.
call npm run backup
echo.
pause
