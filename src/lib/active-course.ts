/**
 * Активный курс (= Category) хранится в cookie, без миграции схемы.
 * Имя cookie вынесено в отдельный простой модуль, потому что его импортируют
 * И серверный экшен ("use server" — там можно экспортировать только async-функции),
 * И серверные страницы (читают cookie напрямую).
 */
export const ACTIVE_COURSE_COOKIE = "activeCourseId";
